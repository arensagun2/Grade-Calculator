export class Calculate {
    constructor(scores, weights) {
        this.scores = scores;
        this.weights = weights;
    }

    get actual() {
        return this.calculate();
    }

    calculate() {
        
    }
}